jQuery(function ($){    $(document).on('submit','#send-location',function (){        var form_data=JSON.stringify($(this).serializeObject());        $.ajax({            url: "http://location/public/index.php",            type : "POST",            contentType : 'application/json',            data : form_data,            success : function(result) {                if (result == 'Validation error') {                    alert(result);                } else {                    var app = document.getElementById('app');                    app.classList.remove('container');                    app.style.width = '100%';                    app.innerHTML = result;                }            },            error: function(xhr, resp, text) {                console.log(xhr, resp, text);            }        });        return false;    });    $(document).on('change','#categories',function () {        var inputs = ``;        if ($(this).val() == "Address") {            inputs = `            <div class="row mb-3" id="inputs">                <label for="address" class="col-sm-2 col-form-label">Адресс</label>                <div class="col-sm-10">                    <input name="address" type="text" class="form-control" id="address">                </div>            </div>`;        } else {            inputs = `            <div class="row mb-3" id="inputs">                <label for="lat" class="col-sm-2 col-form-label">Долгота</label>                <div class="col-sm-10">                    <input name="lat" type="float" min="-200" class="form-control" id="lat">                </div>            </div>            <div class="row mb-3" >                <label for="lon" class="col-sm-2 col-form-label">Широта</label>                <div class="col-sm-10">                    <input name="lon" type="float" min="-200" class="form-control" id="lon">                </div>            </div>`;        }        $('#inputs').html(inputs);    });});$.fn.serializeObject = function() {    var o = {};    var a = this.serializeArray();    $.each(a, function() {        if (o[this.name] !== undefined) {            if (!o[this.name].push) {                o[this.name] = [o[this.name]];            }            o[this.name].push(this.value || '');        } else {            o[this.name] = this.value || '';        }    });    return o;};